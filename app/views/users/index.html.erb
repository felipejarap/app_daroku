<div class="container">
  <div class="row">
     <div class="w-100" style="margin-bottom:20px"> </div>
    <% @products.each_slice(4) do |row| %>
    <% row.each do |product| %>
    <div class="col">
      <div class="card">
        <%= image_tag product.photo, class:'card-img-top' %>
        <div class="body">
          <h5><%=product.name %></h5>
          <p><%= product.brand %><p>
          <p><%= product.description %><p>
          <p><%= product.price %><p>
          <p><%= product.stock %><p>
          <p><%= product.category.description %><p>
            <%= link_to "añadir al carito de compra",product_orders_path(product), class: 'btn btn-success', method: :post %>
        </div>
         </div>
       </div>
      <% end %>
      <div class="w-100" style="margin-top:20px"> </div>
    <% end %>
    </div>
  </div>
</div>

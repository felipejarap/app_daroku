<%= form_with(model: type_suggestion, local: true) do |form| %>
  <% if type_suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_suggestion.errors.count, "error") %> prohibited this type_suggestion from being saved:</h2>

      <ul>
      <% type_suggestion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
